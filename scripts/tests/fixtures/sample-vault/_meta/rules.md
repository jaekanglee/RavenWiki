# Rules (no frontmatter)

This page intentionally has NO frontmatter. The build must fall back to
defaults (type=rule, today's date, etc.) and still index it.

See [[a]] for an example of a concept page.
