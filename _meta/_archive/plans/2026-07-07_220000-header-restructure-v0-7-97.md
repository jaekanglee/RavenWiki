# Header Restructure v0.7.97 — Implementation Plan

> **For Raven dev (user = repo owner, 1인 + web 검증 워크플로우)**
> **Direction (Codex 검토 일치): B안 — 헤더 좌/우 그룹화 + 검색은 사이드바 상단으로 통합.**
> **Surgical 원칙: 한 컴포넌트씩 점진 도입. 한 번에 다 바꾸지 말 것.**

**Goal:** 헤더 위계 정리 (그룹화) + 검색을 사이드바로 이관 + §13 토큰화 위반 정리.

**Tech Stack:** React + react-router-dom, CSS 변수 토큰, surgical 인라인 → 컴포넌트 분리.

---

## Round 1 — 1차 패치 (commit: `feat(dashboard): 헤더 그룹화 + 검색 사이드바 통합`)

### Task 1: SearchBar 컴포넌트 variant 지원 (compact 모드)

**Files:**
- Modify: `dashboard/src/components/SearchBar.tsx`

**Step:**
- `variant?: "header" | "sidebar"` prop 추가 (기본 "header")
- `sidebar` variant: height 40 (vs 64), placeholder `"{vault} 검색…"`, dropdown은 그대로 (combobox 동작 동일)
- border/shadow는 inline → class 토큰화 (이미 `.search-bar-pill` 일부 활용, 더 정리)

**검증:** `npx tsc -b` exit 0, `npx vite build` exit 0

---

### Task 2: Sidebar.tsx — SearchBar 통합 + filter 분리

**Files:**
- Modify: `dashboard/src/components/Sidebar.tsx:293-303` 인근

**Step:**
- 기존 `sidebar-filter-input` 위에 SearchBar (`variant="sidebar"`) 삽입
- 라벨 구분:
  - SearchBar: `"{vault} 검색…"`
  - 필터 input: 그대로 `"파일 또는 폴더 필터..."`
- vertical rhythm (marginBottom 8 또는 12)

**검증:** tsc + build, 사이드바에서 검색 → 페이지 이동 OK, 필터도 정상 동작

---

### Task 3: Layout.tsx — 헤더 그룹화 + SearchBar 제거

**Files:**
- Modify: `dashboard/src/components/Layout.tsx:178-273`

**Step:**
- `<SearchBar>` 호출 제거 (lines 239-248)
- 헤더를 `<div className="header-left">hamburger + brand + vault</div>` + `<nav className="header-right">tabs</nav>` 두 클러스터로 분리
- 컨텐츠 영역(`page-content` line 275-285)에 `maxWidth: 1200, margin: "0 auto"` 추가 (좌측정렬 → 센터 컨테이너)
- 즐겨찾기 hover JS 이벤트 → CSS class로 이전 (Sidebar.tsx:236-270)

**검증:** tsc + build, 헤더 시각 깔끔, 컨텐츠 중앙정렬, 즐겨찾기 hover 자연스러움

---

## Round 2 — 2차 패치 (필요 시, 같은 commit 묶음 가능)

### Task 4: §13 위반 정리 — SearchBar inline 스타일 토큰화

**Files:**
- Modify: `dashboard/src/components/SearchBar.tsx`
- Modify: `dashboard/src/styles/globals.css`

**Step:**
- SearchBar의 `height: 64`, 보더, 그림자, 패딩 등 inline → CSS class 또는 변수로
- variant별 token: `--search-height-header`, `--search-height-sidebar`
- `rgba(...)` 직접 사용 위치 정리 (globals.css 1589-1591, 2182)

**검증:** tsc + build, 시각 동일

---

## Anti-Pattern Checklist (이 plan은 데이터/지식 레이어가 아니라 UI 패치)

| 항목 | 상태 | 비고 |
|---|---|---|
| Multi-file SoT 충돌 | N/A | UI 패치 |
| §13.1 재사용 컴포넌트 | ✅ | SearchBar variant prop, 즐겨찾기 hover CSS class |
| §13.2 색/폰트 토큰 | ✅ | rgba 정리, inline → 변수 |
| Surgical 변경 | ✅ | 한 번에 다 바꾸지 않고 variant/cluster/링크 순서 |
| 사용자 mobile-first | ✅ | ≤744px: 햄버거 + 사이드바 drawer, 헤더는 얇아짐 |
| 5번째 진입점 추가 ❌ | N/A | UI 패치 |
| 회귀 위험 가드 | ✅ | tsc + vite build 매 task 후 |

---

## Risks & Tradeoffs

| 위험 | 완화 |
|---|---|
| active vault 변경 시 검색 포커스/결과 깨짐 | variant prop + key 안정성으로. 회귀 시 1차 패치 롤백 후 2차에서 재시도 |
| 반응형 줄바꿈 깨짐 (top-nav-row 1056-1080) | CSS 함께 수정. 컨테이너 폭 계산 명시 |
| 모바일 drawer 정보 밀도 | Section 간 vertical rhythm 12px → 16px |

## 검증 명령 (각 task 후)

```bash
cd /Users/jaekanglee/Desktop/Dev/Project/Raven/dashboard
npx tsc -b
npx vite build  # background로 띄워 echo 받기
```

## Commit

```bash
git add dashboard/src/components/Layout.tsx dashboard/src/components/Sidebar.tsx dashboard/src/components/SearchBar.tsx dashboard/src/styles/globals.css
git commit -m "feat(dashboard): 헤더 그룹화 + 검색 사이드바 통합 (v0.7.97, B안)"
```